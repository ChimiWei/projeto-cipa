<!DOCTYPE html>
<html>

<head>
    <%- include('partials/head', {title: 'Login' }); %>
</head>



<body class="bg-image">
    <section class="st-section ">
        <div class="container rounded bg-white shadow py-4">
            <div class="row justify-content-center ">
                <div class="col-md-6 text-center mb-5">
                    <h2 class="title fs-1 text-shadow"> CI </h2>
                    <small>Cipa Inova</small>
                </div>
                <div class="row justify-content-center">
                    <div class="col-md-6  text-center">
                        <h4 class=" mb-4"> Insira o email cadastrado na sua conta para receber o seu link de redefinição
                            de senha </h4>
                        <div class="login-wrap p-md-3">

                            <form action="/reset_password" method="POST" class="singin-form">
                                <div class="form-group ">
                                    <input name="email" type="email" placeholder="email para recuperação"
                                        class="form-control text-center mb-4" autocomplete="one-time-code" required>

                                </div>
                                <div class="my-1 ps-2">
                                    <% if (message.error) { %>
                                        <%= message.error %>
                                            <% } %>
                                </div>
                                <div class="form-group text-center">
                                    <button type="submit" class="form-control btn btn-color rounded submit mt-3">
                                        Continuar
                                    </button>
                                </div>
                            </form>
                            <div class="form-group text-center mt-5">
                                <span> Não possui uma conta? <a href="/register">Cadastre-se</a> </span>
                            </div>

                        </div>

                    </div>

                </div>
            </div>
        </div>
        </div>
        <%- include('components/modal/notification.ejs', { message: locals.message }); %>
    </section>

    <script src='js/login.js'></script>
    <script src="https://kit.fontawesome.com/49ddb56ecf.js" crossorigin="anonymous"></script>
</body>

</html>